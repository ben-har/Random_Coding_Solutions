using System.Text;
public class Program
{
    public static bool IsPalindrome(int num)
    {
			StringBuilder sb = new StringBuilder();
			for(int i = num.ToString().Length - 1; i >= 0; i--)
			{
				sb.Append(num.ToString()[i]);
			}
			return int.Parse(sb.ToString()) == num;
    }
}
